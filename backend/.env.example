# API settings
PROJECT_NAME="Support Ticket Intelligence API"
API_V1_STR="/api/v1"

# CORS Origins (comma separated list or JSON string, pydantic handles lists natively if formatted correctly)
BACKEND_CORS_ORIGINS='["http://localhost:3000"]'
